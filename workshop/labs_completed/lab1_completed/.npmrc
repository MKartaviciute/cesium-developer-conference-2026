# Use a flat node_modules layout (real directories instead of symlinks/junctions).
# Turbopack on Windows does not reliably resolve packages through pnpm's
# symlinked node_modules, which caused: "We couldn't find the Next.js package
# (next/package.json)". Hoisting makes packages directly resolvable.
node-linker=hoisted
