# Marks lab1 as a standalone pnpm workspace root.
# This prevents pnpm from climbing up to the monorepo root when running
# `pnpm install` inside this directory.
packages:
  - "packages/*"
allowBuilds:
  esbuild: true
  protobufjs: true
  sharp: true
  unrs-resolver: true
